doctype html
html
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title= title
        script(src="https://cdn.tailwindcss.com")
    body(class="bg-gray-50")
        div(class="container mx-auto px-4 py-8 max-w-6xl")
            h1(class="text-3xl font-bold mb-6") Anime Characters Table
            
            a(href="/update-cobj" class="inline-block px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 mb-6") 
                | Add New Character

            if data && data.length
                div(class="overflow-x-auto bg-white rounded-lg shadow")
                    table(class="min-w-full")
                        thead(class="bg-gray-50")
                            tr
                                th(class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider") Name
                                th(class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider") Anime
                                th(class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider") Age
                        tbody(class="divide-y divide-gray-200")
                            each item in data
                                tr(class="hover:bg-gray-50")
                                    td(class="px-6 py-4 whitespace-nowrap")= item.properties.name
                                    td(class="px-6 py-4 whitespace-nowrap")= item.properties.anime
                                    td(class="px-6 py-4 whitespace-nowrap")= item.properties.age
            else
                div(class="text-center py-12 bg-white rounded-lg shadow")
                    p(class="text-gray-500") No anime characters found.